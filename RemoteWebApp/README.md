# RemoteWebApp

Static web app (HTML/CSS/JS) served in-LAN by `RemoteControlServer` for
mobile remote control. Not yet scaffolded — blocked on PROMPT MASTER §10
checkpoint 1 (HTTP server library choice), since the frontend's build/serve
setup depends on how the backend serves static assets.

Planned: `index.html` entry point, pairing flow (scan QR shown by the Mac
app, save per-device token), disk status view, pending-approval list.
