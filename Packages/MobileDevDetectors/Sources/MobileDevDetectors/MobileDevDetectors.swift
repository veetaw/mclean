/// Placeholder so this target builds before `Agent:MobileDevDetectors`
/// (Phase 1) fills it in with `CoreScanEngine.Detector` conformances for
/// Android (AVDs, Gradle cache, SDK) and iOS/watchOS/tvOS (simulators,
/// CocoaPods, Fastlane) — see PROMPT MASTER §5.3.
public enum MobileDevDetectorsModule {
    public static let placeholder = true
}
