import XCTest
@testable import MobileDevDetectors

/// Placeholder so the MobileDevDetectorsTests target builds. Real tests land with the
/// module's Phase 1/2/3 implementation.
final class MobileDevDetectorsPlaceholderTests: XCTestCase {
    func testTargetBuilds() {
        XCTAssertTrue(true)
    }
}
