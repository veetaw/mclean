/// Placeholder so this target builds before `Agent:OptimizationModule`
/// (Phase 6) fills it in — login items/launch agents management, startup
/// impact report. See PROMPT MASTER §5.1.
public enum OptimizationModule {
    public static let placeholder = true
}
