import XCTest
@testable import VirusTotalClient

/// Placeholder so the VirusTotalClientTests target builds. Real tests land with the
/// module's Phase 1/2/3 implementation.
final class VirusTotalClientPlaceholderTests: XCTestCase {
    func testTargetBuilds() {
        XCTAssertTrue(true)
    }
}
