/// Placeholder so this target builds before `Agent:UIDesignSystem` (Phase 1)
/// fills it in — shared Liquid Glass components and design tokens (see the
/// `apple-liquid-glass` skill for API guidance) used across the main app,
/// menu bar popover, and onboarding flows.
public enum UIDesignSystemModule {
    public static let placeholder = true
}
