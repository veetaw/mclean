/// Placeholder so this target builds before `Agent:CacheCleaner` (Phase 6)
/// fills it in — user/system cache, logs, temp files, incomplete
/// downloads, unused language packs. See PROMPT MASTER §5.1.
public enum CacheCleanerModule {
    public static let placeholder = true
}
