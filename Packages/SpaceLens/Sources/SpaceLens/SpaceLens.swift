/// Placeholder so this target builds before `Agent:SpaceLens` (Phase 6)
/// fills it in — interactive disk-usage treemap, read-only. See PROMPT
/// MASTER §5.1.
public enum SpaceLensModule {
    public static let placeholder = true
}
