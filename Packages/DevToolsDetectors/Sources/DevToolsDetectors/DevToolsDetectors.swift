/// Placeholder so this target builds before `Agent:DevToolsDetectors` (Phase 1)
/// fills it in with real `CoreScanEngine.Detector` conformances. See README.md.
public enum DevToolsDetectorsModule {
    public static let placeholder = true
}
