import XCTest
@testable import DevToolsDetectors

/// Placeholder so the DevToolsDetectorsTests target builds. Real tests land with the
/// module's Phase 1/2/3 implementation.
final class DevToolsDetectorsPlaceholderTests: XCTestCase {
    func testTargetBuilds() {
        XCTAssertTrue(true)
    }
}
