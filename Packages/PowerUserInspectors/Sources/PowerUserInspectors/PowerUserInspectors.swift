/// Placeholder so this target builds before `Agent:PowerUserInspectors`
/// (Phase 2) fills it in — installed apps + TCC permission listing, config
/// file explorer, per-language package explorer. See PROMPT MASTER §5.4.
public enum PowerUserInspectorsModule {
    public static let placeholder = true
}
