import XCTest
@testable import PowerUserInspectors

/// Placeholder so the PowerUserInspectorsTests target builds. Real tests land with the
/// module's Phase 1/2/3 implementation.
final class PowerUserInspectorsPlaceholderTests: XCTestCase {
    func testTargetBuilds() {
        XCTAssertTrue(true)
    }
}
