/// Placeholder so this target builds before `Agent:DuplicateFinder`
/// (Phase 5) fills it in — exact-match hashing for generic files,
/// perceptual hashing for similar images. See PROMPT MASTER §5.1.
public enum DuplicateFinderModule {
    public static let placeholder = true
}
