/// Placeholder so this target builds before `Agent:TrashCleaner` (Phase 5)
/// fills it in — Finder Trash + known third-party trash bins (Mail,
/// Photos, major browsers). See PROMPT MASTER §5.1.
public enum TrashCleanerModule {
    public static let placeholder = true
}
