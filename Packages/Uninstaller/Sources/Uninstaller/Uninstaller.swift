/// Placeholder so this target builds before `Agent:UninstallerModule`
/// (Phase 6) fills it in — app + related-file removal with full preview.
/// See PROMPT MASTER §5.1.
public enum UninstallerModule {
    public static let placeholder = true
}
