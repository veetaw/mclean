/// Placeholder so this target builds before `Agent:LargeOldFilesFinder`
/// (Phase 5) fills it in — configurable size/age-threshold filesystem scan.
/// See PROMPT MASTER §5.1.
public enum LargeOldFilesFinderModule {
    public static let placeholder = true
}
