import XCTest
@testable import MenuBarAgent

/// Placeholder so the MenuBarAgentTests target builds. Real tests land with the
/// module's Phase 1/2/3 implementation.
final class MenuBarAgentPlaceholderTests: XCTestCase {
    func testTargetBuilds() {
        XCTAssertTrue(true)
    }
}
