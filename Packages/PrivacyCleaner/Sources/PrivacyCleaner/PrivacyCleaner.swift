/// Placeholder so this target builds before `Agent:PrivacyCleaner`
/// (Phase 6) fills it in — browser cache/cookie/history detectors with a
/// site preserve-list. See PROMPT MASTER §5.1.
public enum PrivacyCleanerModule {
    public static let placeholder = true
}
