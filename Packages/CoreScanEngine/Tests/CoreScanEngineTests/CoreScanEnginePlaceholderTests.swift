import XCTest
@testable import CoreScanEngine

/// Placeholder so the CoreScanEngineTests target builds. Real tests land with the
/// module's Phase 1/2/3 implementation.
final class CoreScanEnginePlaceholderTests: XCTestCase {
    func testTargetBuilds() {
        XCTAssertTrue(true)
    }
}
