/// Placeholder — blocked on PROMPT MASTER §10 checkpoint 1 (HTTP server
/// library choice) before `Agent:RemoteControlServer` (Phase 2) implements
/// pairing, auth, and the status/approval endpoints. See PROMPT MASTER §5.6.
public enum RemoteControlServerModule {
    public static let placeholder = true
}
